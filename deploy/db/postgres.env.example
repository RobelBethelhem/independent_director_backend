# Copy to postgres.env (same folder) and fill in real values on the Database
# Server. NEVER commit the real postgres.env — it's gitignored.
#
# User/password must match DATABASE_URL in ../onprem/backend.env.

POSTGRES_USER=REPLACE_ME
POSTGRES_PASSWORD=REPLACE_ME_LONG_RANDOM_SECRET
POSTGRES_DB=zemen_director_portal
