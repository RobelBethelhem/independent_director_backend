# Copy to minio.env (same folder) and fill in real values on the Application
# Server. NEVER commit the real minio.env — it's gitignored.
#
# These MUST match S3_ACCESS_KEY / S3_SECRET_KEY in deploy/onprem/backend.env.

MINIO_ROOT_USER=REPLACE_ME
MINIO_ROOT_PASSWORD=REPLACE_ME_LONG_RANDOM_SECRET
